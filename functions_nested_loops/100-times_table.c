#include "main.h"
/**
 * print_times_table  - prints the table
 * @n: number to print
 * Return: 1 sucess or 0 in fail
 */
void print_times_table(int n)
{
	int x, y, res;
	if (n < 0 || n > 15)
	{
		_putchar(10);
		return;
	}
	for 
}
