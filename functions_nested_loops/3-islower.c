#include "main.h"
#include <ctype.h>
/**
 * _islower - this function check if it is lowercase
 * @c: the caracter to print
 * Return: ALways 0
 */
int _islower(int c)
{
	char i;

	if (islower(i))
		_putchar(48);
	else
		_putchar(49);
	return (c);
}
